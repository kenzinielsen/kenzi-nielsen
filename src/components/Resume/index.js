import React from "react";

function Resume() {
    return (

        <section>
            <h1>
            <img src={require(`../../assets/images/resume.png`).default} />
            </h1>
        </section>

    )
}

export default Resume;